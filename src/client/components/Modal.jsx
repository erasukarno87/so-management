// Modal - Re-export from modals folder for backward compatibility
// Split into: ViewModal, FormModal, DeleteModal, ConfirmModal

import { ViewModal, FormModal, DeleteModal, ConfirmModal } from './modals/index.js';

export { ViewModal, FormModal, DeleteModal, ConfirmModal };

export default {
  ViewModal,
  FormModal,
  DeleteModal,
  ConfirmModal,
};